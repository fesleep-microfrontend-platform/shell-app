@fesleep-microfrontend-platform:registry=https://npm.pkg.github.com
# Auth: npm login --registry=https://npm.pkg.github.com
# Copy (not symlink) local file: deps, so each linked package resolves
# Angular from this project's own node_modules instead of its own nested copy.
install-links=true
